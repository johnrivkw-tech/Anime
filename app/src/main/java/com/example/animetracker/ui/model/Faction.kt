package com.example.animetracker.ui.model

/** Which side of the One Piece world the user's rank ladder is drawn from. */
enum class Faction(val displayName: String) {
    PIRATE("Pirate"),
    MARINE("Marine")
}
